name: Retired H.40 run locator

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-22.04
    steps:
      - name: Explain retirement
        run: |
          echo "This self-mutating V4.2 locator is retired."
          echo "Use the GitHub Actions run page as the authoritative build status."
